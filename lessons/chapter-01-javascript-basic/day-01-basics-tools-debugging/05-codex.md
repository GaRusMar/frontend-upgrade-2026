# Day 01 — Codex

## Что можно отдать Codex

Codex можно попросить:

- создать структуру папки урока;
- создать `index.html`;
- создать `style.css`;
- создать пустой `script.js` с TODO;
- проверить мой код после самостоятельной реализации;
- объяснить ошибку из консоли;
- предложить рефакторинг после того, как я сам написал решение;
- проверить, не забыл ли я edge cases.

## Что студент пишет сам

Самостоятельно пишу:

- поиск элементов через `querySelector`;
- обработчик `submit`;
- `event.preventDefault()`;
- чтение `input.value`;
- преобразование через `Number()`;
- проверки через `if`;
- проверку `Number.isNaN`;
- расчёт `itemsTotal`;
- расчёт `discount`;
- расчёт `delivery`;
- расчёт `finalTotal`;
- вывод результата через `textContent`;
- обработку ошибок.

## Что нельзя отдавать Codex

Codex нельзя просить:

- полностью написать `script.js`;
- написать калькулятор за меня;
- исправить код без объяснения причины;
- заменить моё понимание готовым решением;
- коммитить код, который я не могу объяснить.

## Workflow

```text
1. Прочитать 01-task.md
2. Прочитать 02-theory.md
3. Выполнить 03-practice.md
4. Написать JavaScript самостоятельно
5. Проверить все сценарии в браузере
6. Ответить на вопросы из 04-questions.md
7. Только после этого просить Codex сделать review
8. Исправить ошибки
9. Сделать коммит
```

## Коммит

```bash
git status
git add lessons/day-01-basics-tools-debugging
git commit -m "feat(jsbasic): implement day 01 price calculator"
```
